main = putStrLn mainCode >> putStr codeCode >> print code where mainCode = head code; codeCode = head $ tail code
code = ["main = putStrLn mainCode >> putStr codeCode >> print code where mainCode = head code; codeCode = head $ tail code","code = "]
